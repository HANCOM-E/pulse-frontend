const ReportPanel = () => {
  // API(openapi v0.3): POST /events/{eventCode}/report/generate.
  // lib/api/endpoints.ts의 generateReport(eventCode)가 이미 구현·테스트돼 있습니다(PR #12).
  // 실제 구현(props 포함)은 축 3(안치호)이 담당합니다.
  return <div>리포트 패널 (준비 중)</div>;
};

export default ReportPanel;
